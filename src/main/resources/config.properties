# ====================================
# INSIDER TEST AUTOMATION FRAMEWORK
# Configuration Properties
# ====================================

# Browser Configuration
browser=chrome
headless=false
maximize.window=true
window.size=1920,1080

# Environment Configuration
environment=local
base.url=https://useinsider.com

# Selenium Grid Configuration (for Docker/Remote execution)
selenium.hub.url=http://selenium-hub:4444/wd/hub

# Timeout Configuration (in seconds)
timeout.implicit=3
timeout.explicit=8
timeout.pageLoad=15
timeout.default=5

# Test Data Configuration
test.data.path=src/test/resources/testdata/
screenshot.path=target/screenshots/
report.path=target/reports/

# Logging Configuration
log.level=INFO
log.console=true
log.file=true
log.file.path=target/logs/

# Allure Configuration
allure.results.directory=target/allure-results
allure.report.directory=target/allure-report

# Application Specific URLs
careers.page.url=https://useinsider.com/careers/
qa.jobs.url=https://useinsider.com/careers/quality-assurance/

# Test Configuration
retry.count=1
parallel.tests=false
thread.count=1

# Database Configuration (if needed)
db.url=
db.username=
db.password=

# API Configuration (if needed)
api.base.url=
api.timeout=30

# Performance Configuration
page.load.strategy=normal
element.highlight=false
screenshot.on.failure=true
video.recording=false

# CI/CD Configuration
ci.environment=false
headless.ci=true
parallel.ci=true

# =============================================================================
# BROWSER CONFIGURATION
# =============================================================================
# Desteklenen browser türleri: chrome, firefox, edge, safari, remote
browser=chrome
headless=false

# =============================================================================
# TIMEOUT CONFIGURATION
# =============================================================================
# Sayfa yükleme timeout (saniye)
page.load.timeout=30
# Implicit wait timeout (saniye)
implicit.wait=10
# Explicit wait timeout (saniye)
explicit.wait=30
# Script timeout (saniye)
script.timeout=30

# =============================================================================
# URL CONFIGURATION
# =============================================================================
# Base URL
base.url=https://useinsider.com
# Career sayfası URL'i
career.url=https://useinsider.com/careers/
# Job listing sayfası URL'i
jobs.url=https://useinsider.com/careers/quality-assurance/

# =============================================================================
# SELENIUM GRID CONFIGURATION
# =============================================================================
# Selenium Grid URL (remote browser için)
selenium.grid.url=http://localhost:4444/wd/hub
# Remote browser türü
remote.browser=chrome
# Remote platform
remote.platform=ANY
# Remote browser version
remote.version=

# =============================================================================
# TEST DATA CONFIGURATION
# =============================================================================
# Test verileri dosya yolu
test.data.path=src/test/resources/testdata/
# Screenshot dosya yolu
screenshot.path=target/screenshots/
# Test raporları dosya yolu
report.path=target/reports/

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================
# Log seviyesi: DEBUG, INFO, WARN, ERROR, FATAL
log.level=INFO
# Log dosya yolu
log.file.path=target/logs/
# Log dosya adı
log.file.name=test-automation.log

# =============================================================================
# ALLURE CONFIGURATION
# =============================================================================
# Allure results dizini
allure.results.directory=target/allure-results/
# Allure report dizini
allure.report.directory=target/allure-report/
# Allure history dizini
allure.history.directory=target/allure-history/

# =============================================================================
# TEST EXECUTION CONFIGURATION
# =============================================================================
# Paralel test çalıştırma
parallel.execution=true
# Thread sayısı
thread.count=3
# Test suite dosya yolu
test.suite.path=testng.xml
# Retry count (başarısız testler için)
retry.count=1

# =============================================================================
# BROWSER OPTIONS CONFIGURATION
# =============================================================================
# Browser window boyutu
browser.width=1920
browser.height=1080
# Browser window pozisyonu
browser.x=0
browser.y=0
# Browser language
browser.language=tr
# Browser timezone
browser.timezone=Europe/Istanbul

# =============================================================================
# PERFORMANCE CONFIGURATION
# =============================================================================
# Network throttling (ms)
network.throttling=0
# CPU throttling (1-4)
cpu.throttling=1
# Memory limit (MB)
memory.limit=512

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================
# SSL sertifika doğrulama
ssl.verification=true
# Proxy kullanımı
use.proxy=false
# Proxy host
proxy.host=
# Proxy port
proxy.port=
# Proxy username
proxy.username=
# Proxy password
proxy.password=

# =============================================================================
# MOBILE CONFIGURATION
# =============================================================================
# Mobile emulation
mobile.emulation=false
# Mobile device name
mobile.device=iPhone 12
# Mobile user agent
mobile.user.agent=

# =============================================================================
# API CONFIGURATION
# =============================================================================
# API base URL
api.base.url=https://api.useinsider.com
# API timeout (saniye)
api.timeout=30
# API key
api.key=

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Database URL
db.url=jdbc:mysql://localhost:3306/testdb
# Database username
db.username=testuser
# Database password
db.password=testpass
# Database driver
db.driver=com.mysql.cj.jdbc.Driver

# =============================================================================
# EMAIL CONFIGURATION
# =============================================================================
# Email gönderimi
send.email=false
# SMTP host
smtp.host=smtp.gmail.com
# SMTP port
smtp.port=587
# SMTP username
smtp.username=
# SMTP password
smtp.password=
# Email alıcıları (virgülle ayrılmış)
email.recipients=

# =============================================================================
# SLACK CONFIGURATION
# =============================================================================
# Slack bildirimi
send.slack=false
# Slack webhook URL
slack.webhook.url=
# Slack channel
slack.channel=#test-automation

# =============================================================================
# JIRA CONFIGURATION
# =============================================================================
# JIRA entegrasyonu
jira.integration=false
# JIRA URL
jira.url=https://jira.company.com
# JIRA username
jira.username=
# JIRA password
jira.password=
# JIRA project key
jira.project.key=TEST

# =============================================================================
# CUSTOM CONFIGURATION
# =============================================================================
# Özel ayarlar buraya eklenebilir
custom.setting1=value1
custom.setting2=value2 